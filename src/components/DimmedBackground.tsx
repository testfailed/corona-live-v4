import React from "react";

import { styled } from "@styles/stitches.config";

const DimmedBackground: React.FC = (props) => {
  return <Wrapper></Wrapper>;
};

const Wrapper = styled("div", {});

export default DimmedBackground;
